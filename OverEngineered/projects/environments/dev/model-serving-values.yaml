replicaCount: 1

image:
  repository: ghcr.io/example/mlops-platform/serving
  tag: 0.1.0
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi

env:
  MODEL_DIR: /models
