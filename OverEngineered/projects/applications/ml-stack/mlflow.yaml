# MLflow tracking server (local chart under projects/charts/mlflow)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mlops-mlflow
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: mlops-platform
    mlops.platform/layer: ml-stack
  annotations:
    argocd.argoproj.io/sync-wave: "20"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: mlops
  source:
    repoURL: https://github.com/example/mlops-platform.git
    targetRevision: HEAD
    path: projects/charts/mlflow
    helm:
      releaseName: mlops-mlflow
      valueFiles:
        - ../../environments/dev/mlflow-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: mlops-shared
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
