{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "model-serving.serviceAccountName" . }}
  labels:
    {{- include "model-serving.labels" . | nindent 4 }}
{{- end }}
