User onboarded successfully.

  User:      {{ include "mlops-user.name" . }}
  Team:      {{ .Values.user.team }}
  Namespace: {{ include "mlops-user.namespace" . }}
  Profile:   {{ .Values.profile }}
  Workload SA: {{ include "mlops-user.serviceAccountName" . }}

Example training job:
  kubectl -n {{ include "mlops-user.namespace" . }} apply -f your-job.yaml

Switch context:
  kubectl config set-context --current --namespace={{ include "mlops-user.namespace" . }}
