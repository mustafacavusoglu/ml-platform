apiVersion: v1
kind: Service
metadata:
  name: mlops-readmes-mcp
  labels:
    app: mlops-readmes-mcp
spec:
  selector:
    app: mlops-readmes-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
